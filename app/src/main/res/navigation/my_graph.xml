<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_graph"
    app:startDestination="@id/fragment_search"
    >

    <fragment
        android:id="@+id/fragment_search"
        android:name="com.dandycat.examarch.view.fragment.SearchFragment"
        tools:layout="@layout/fragment_search"
        >

        <action
            android:id="@+id/action_fragment_search_to_fragment_repo"
            app:destination="@id/fragment_repo" />
    </fragment>

    <fragment
        android:id="@+id/fragment_repo"
        android:name="com.dandycat.examarch.view.fragment.RepoFragment"
        tools:layout="@layout/fragment_repo"
        />

</navigation>